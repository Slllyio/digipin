[pytest]
testpaths = tests scrapers/sources/tests pipeline guna-twin-city/analysis/tests
pythonpath = .
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    unit: fast unit tests
    integration: slower tests that hit external services
