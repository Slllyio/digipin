{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/mcp-config.schema.json",
  "mcpServers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "description": "Live documentation lookup for MapLibre, georaster, Open-Meteo, IMD, and the other libraries the portal depends on. Use this in place of WebFetch for library docs — the docs change frequently and context7 stays current."
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "description": "Browser automation for testing the cell panel, flood animation, and inundation overlay. Project already has playwright as a dep (record-video.mjs uses it) so the binary is on hand."
    }
  }
}
