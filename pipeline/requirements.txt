# DigiPin Digital Twin — Pipeline Dependencies
# pip install -r requirements.txt

# Core geospatial
rasterio>=1.3.10
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
fiona>=1.9

# Raster processing
rio-cogeo>=5.0
numpy>=1.26

# Vector tiling (tippecanoe installed separately via OS package)
# sudo apt install tippecanoe  OR  brew install tippecanoe

# Data fetching
requests>=2.31
earthaccess>=0.9
pystac-client>=0.7

# Cloud storage
boto3>=1.34

# Analysis
duckdb>=0.10
pyarrow>=15.0
pandas>=2.1

# OSM
osmnx>=1.9

# Optional: Sentinel-2 processing
# stackstac>=0.5
# xarray>=2024.1
